import { LoadingOutlined } from '@ant-design/icons'
const Loading = ({ color = '#d980fa', fontSize = 40 }) => {
    return <LoadingOutlined style={{ color: color, fontSize: fontSize }} />
}

export default Loading
